'use client';
import FaceTime from "@/components/apps/FaceTime";

const FaceTimeApp = () => {
    return (
       <div className="text-white bg-black h-full w-full flex flex-col pt-10">
         <FaceTime />
       </div>
    );
};

export default FaceTimeApp;
